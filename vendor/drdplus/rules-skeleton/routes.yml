with_path_and_query:
  path: /{path}?{query}
tables:
  path: /{path}
  requirements:
    path: '(tabulky|tables)/*'
  defaults:
    query: ''
with_query:
  path: /?{query}
  defaults:
    path: ''
root:
  path: /{path}
  defaults:
    path: ''
    query: ''
